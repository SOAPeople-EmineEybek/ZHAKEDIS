managed with additional save implementation in class ZBP_I_MMT_INTERTYPE_S unique;
strict;
with draft;
define behavior for ZI_MMT_INTERTYPE_S alias ZMMT_INTERTYPE_S
draft table ZMMT_INTERTYPE_S
with unmanaged save
lock master total etag LastChangedAtMax
authorization master( global )
{
  field ( readonly )
   SingletonID;

  field ( features : instance )
   TransportRequestID;

  field ( notrigger )
   SingletonID,
   LastChangedAtMax;


  update;
  internal create;
  internal delete;

  draft action ( features : instance ) Edit with additional implementation;
  draft action Activate optimized;
  draft action Discard;
  draft action Resume;
  draft determine action Prepare;
  action ( features : instance ) SelectCustomizingTransptReq parameter D_SelectCustomizingTransptReqP result [1] $self;

  association _ZMMT_INTERTYPE_D { create ( features : instance ); with draft; }

  validation ValidateTransportRequest on save ##NOT_ASSIGNED_TO_DETACT
  {
    create;
    update;
  }

  side effects
  {
    action SelectCustomizingTransptReq affects
        $self;
  }

}

define behavior for ZI_MMT_INTERTYPE_D alias ZMMT_INTERTYPE_D ##UNMAPPED_FIELD
persistent table ZMMT_INTER_TYPE
draft table ZMMT_INTERTYPE_D
lock dependent by _ZMMT_INTERTYPE_S
authorization dependent by _ZMMT_INTERTYPE_S
{
  field ( mandatory : create )
   Interruptiontype;

  field ( readonly )
   SingletonID;

  field ( readonly : update )
   Interruptiontype;

  field ( notrigger )
   SingletonID;


  update( features : global );
  delete( features : global );

  factory action ( features : instance ) Copy parameter ZD_MMT_INTER_TYPE_P [1];

  mapping for ZMMT_INTER_TYPE
  {
    Interruptiontype = INTERRUPTIONTYPE;
    Interruptiontypetxt = INTERRUPTIONTYPETXT;
    CustomField = CUSTOM_FIELD;
    SignIndicator = SIGN_INDICATOR;
    TaxEntryReq = TAX_ENTRY_REQ;
    InvoiceReq = INVOICE_REQ;
    Reflection = REFLECTION;
    Taxcode = TAXCODE;
    AccDocType = ACC_DOC_TYPE;
    AccDocTypeNeg = ACC_DOC_TYPE_NEG;
    PypSuffix = PYP_SUFFIX;
    WbsReq = WBS_REQ;
  }

  association _ZMMT_INTERTYPE_S { with draft; }

  validation ValidateTransportRequest on save ##NOT_ASSIGNED_TO_DETACT
  {
    create;
    update;
    delete;
  }

}